# EXPORT REACT

Generate:
- readable source;
- router/routes;
- runtime components;
- themes/styles/breakpoints;
- data/query adapters;
- forms/actions/state only when used;
- assets;
- package manifest/lock strategy;
- README;
- env example.

Must consult RuntimeDependencyManifest.
Must install/typecheck/build in fixture.
Puck editor never ships by default.
