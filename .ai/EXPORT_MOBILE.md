# EXPORT MOBILE

Primary: React Native + Expo.

Generate:
- Expo project;
- Expo Router routes;
- SQLite/Drizzle;
- Native renderer;
- state/actions/forms/data;
- used permissions/modules only;
- assets;
- app config;
- source instructions.

Navigation:
stable Stack / standard JS Tabs.
No unstable native tabs default.

Android:
source + APK/AAB when toolchain/signing available.

iOS:
source/Xcode; IPA only with macOS/signing.

Fallback:
Capacitor, explicit user choice/recommendation.
