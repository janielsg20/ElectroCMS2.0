# M08.7 — Implementar workflow editorial y revisiones de Records

**Fase propietaria:** F08 — CMS Schema, Content Store, Workflow e Indexer

## Por qué existe
Conservar capacidades CMS profesionales sin introducir un workflow engine separado.

## Resultado exacto
Al cerrar esta microfase, **Implementar workflow editorial y revisiones de Records** debe quedar integrado, persistido/exportable cuando corresponda y cubierto por pruebas. No basta con un tipo, un botón o un placeholder.

## Engine owner
`PGlite/Drizzle + ElectroSchema`

## Responsabilidad ElectroCMS
Construye únicamente la semántica portable, adapter, configuración, UX y diagnostics que el engine no trae. No dupliques el motor.

## Ubicación visual
Administrar > Modelos/Contenido/Taxonomías/Relaciones.

## Precondiciones
- La microfase anterior de la fase está COMPLETADA o la dependencia está documentada como ya disponible.
- Los contratos canónicos necesarios existen.
- El engine owner está encapsulado detrás del adapter correspondiente.

## Implementación línea por línea
1. Añade a ContentType `workflow.statuses` y `workflow.transitions` configurables; default simple Draft/Published/Archived y optional Pending Review.
2. Cada transition declara capability/role requirement y optional ActionGraph trigger.
3. Record status change pasa por ContentService y ElectroPermissionEvaluator.
4. Usa `audit_events`/Refine AuditLogProvider-compatible data para previousData/newData safe snapshots o field diffs; no construyas un revision DB engine paralelo.
5. Record History muestra eventos y permite `Restore this version` creando una nueva update, nunca reescribiendo audit history.
6. Public listings/query defaults deben poder filtrar Published según visibility policy.
7. Prueba Author -> Pending -> Editor Publish y denied transition.

## Artefactos obligatorios al cerrar
- `ContentWorkflowDefinition`
- `RecordWorkflowService`
- `RecordHistoryPanel`
- `workflow/audit tests`

## Estados y errores
- Define empty/loading/error/disabled/blocked cuando tengan sentido.
- Un caso unsupported nunca se convierte silenciosamente en success.
- Diagnostics deben incluir código, ubicación, causa y acción sugerida.

## Responsive y accesibilidad si toca UI
- Desktop high-density sin overflow.
- Tablet usa Sheet/Drawer para tooling secundario.
- Mobile usa route/card/bottom-sheet, no desktop comprimido.
- Keyboard/focus-visible/aria-label.
- Alternative a DnD/gestos.

## Tests obligatorios
1. Unit/contract del valor Electro.
2. Integration con engine/storage real.
3. Negative/failure path.
4. Round-trip si persiste canonical data.
5. E2E si existe UI.
6. Target fixture si afecta export/native/server.
7. lint.
8. typecheck.
9. test.
10. build.

## Prohibido
- No crear subsystem paralelo.
- No persistir engine internals como modelo canónico.
- No usar mocks permanentes.

## Evidencia TRACKING
- archivos modificados;
- engine/API usado;
- pruebas ejecutadas;
- build/fixture;
- blockers/adaptations;
- siguiente microfase.
