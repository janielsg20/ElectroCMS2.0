# M22.8 — Generar professionalStudio manifest portable

**Fase propietaria:** F22 — ElectroExportIR, Local/React/Static/PWA y Dependency Pruning

## Por qué existe
Preservar modelos y capacidades avanzadas junto con cada export sin obligar al runtime a cargar el Studio.

## Resultado exacto
Al cerrar esta microfase, **Generar professionalStudio manifest portable** debe quedar integrado, persistido/exportable cuando corresponda y cubierto por pruebas. No basta con un tipo, un botón o un placeholder.

## Engine owner
`ElectroExportIR + target generators`

## Responsabilidad ElectroCMS
Construye únicamente la semántica portable, adapter, configuración, UX y diagnostics que el engine no trae. No dupliques el motor.

## Ubicación visual
Publicar > Exportar y generated output reports.

## Precondiciones
- La microfase anterior de la fase está COMPLETADA o la dependencia está documentada como ya disponible.
- Los contratos canónicos necesarios existen.
- El engine owner está encapsulado detrás del adapter correspondiente.

## Implementación línea por línea
1. Genera `professionalStudio` manifest versionado dentro de Local/React/LAMP/WordPress exports y opcionalmente Native source.
2. Incluye schema refs, relations, operations, template/admin screen metadata, capability IDs, data migration metadata y source Electro version.
3. NO incluye Puck/Rete editor state, workspace preferences, secrets ni binary assets inline.
4. El manifest sirve para reimport, diagnostics, future editing/migration y equivalence tests.
5. Target runtime puede ignorarlo en ejecución si no lo necesita.
6. WordPress/LAMP import/reverse mapping tests verifican que no se pierda semántica profesional.
7. Documenta manifest schema/migrations.
8. Compatibility report referencia los same capability IDs.

## Artefactos obligatorios al cerrar
- `professionalStudio.schema.json`
- `manifest generator/tests`

## Estados y errores
- Define empty/loading/error/disabled/blocked cuando tengan sentido.
- Un caso unsupported nunca se convierte silenciosamente en success.
- Diagnostics deben incluir código, ubicación, causa y acción sugerida.

## Responsive y accesibilidad si toca UI
- Desktop high-density sin overflow.
- Tablet usa Sheet/Drawer para tooling secundario.
- Mobile usa route/card/bottom-sheet, no desktop comprimido.
- Keyboard/focus-visible/aria-label.
- Alternative a DnD/gestos.

## Tests obligatorios
1. Unit/contract del valor Electro.
2. Integration con engine/storage real.
3. Negative/failure path.
4. Round-trip si persiste canonical data.
5. E2E si existe UI.
6. Target fixture si afecta export/native/server.
7. lint.
8. typecheck.
9. test.
10. build.

## Prohibido
- No crear subsystem paralelo.
- No persistir engine internals como modelo canónico.
- No usar mocks permanentes.

## Evidencia TRACKING
- archivos modificados;
- engine/API usado;
- pruebas ejecutadas;
- build/fixture;
- blockers/adaptations;
- siguiente microfase.
